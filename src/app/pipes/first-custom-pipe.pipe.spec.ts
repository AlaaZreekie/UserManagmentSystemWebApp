import { FirstCustomPipePipe } from './first-custom-pipe.pipe';

describe('FirstCustomPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FirstCustomPipePipe();
    expect(pipe).toBeTruthy();
  });
});
